services:
  board:
    build: .
    image: codex-miro:local
    container_name: codex-miro-board
    ports:
      - "8080:8080"
    environment:
      WORKSPACE_DIR: /app/workspace
    volumes:
      - board_data:/app/workspace
    restart: unless-stopped

volumes:
  board_data:

{
  "name": "zmtki",
  "version": "0.2.0",
  "description": "zmtki — a lightweight local whiteboard with REST and MCP access.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "mcp": "node src/mcp.js",
    "check": "node --check src/server.js && node --check src/mcp.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}

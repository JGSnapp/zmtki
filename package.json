{
  "name": "codex-miro",
  "version": "0.1.0",
  "description": "A lightweight local whiteboard with REST and MCP access.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "mcp": "node src/mcp.js",
    "check": "node --check src/server.js && node --check src/mcp.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
